import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  basicStyle: {
    fontSize: 14,
    color: '#000',
    borderWidth: 1,
    borderColor: '#000',
    padding: 10,
    borderRadius: 10,
  },
  textStyle: {
    textAlign: 'center',
  },
});

export default styles;
